import React from 'react';
import * as A from '../styles/ARStyle';

const ARScreen = () => {
    return (
        <A.Screen>
            <A.Text>AR페이지</A.Text>
        </A.Screen>
    );
};

export default ARScreen;
